import 'package:adpv_frontend/Models/EndpointData.dart';
import 'package:flutter/material.dart';

class EndpointInfoTable extends StatelessWidget {
  const EndpointInfoTable({Key? key, required this.endpointData}) : super(key: key);

  final EndpointData endpointData;
  @override
  Widget build(BuildContext context) {
    return Container();
  }

}
